# Ficheros automaticos

Los ficheros que acaban en .g.dart son generados automaticamente por el paquete "riverpod_generator". No deben ser modificados.

```bash
# Para generarlos: 
flutter pub run build_runner build --delete-conflicting-outputs
```
