import 'package:flutter/material.dart';

/// Configuración de estilo para las tarjetas de categoría en la sección de categorías.
class CategoryStyleConfig {
  final IconData icon;
  final Color color;

  const CategoryStyleConfig(this.icon, this.color);
}
